[package]
name = "rust-publisher"
version = "0.1.0"
edition = "2021"

[dependencies]
livekit = { version = "0.8.4", features = ["native-tls"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
